import { UserTimingTracking } from './user-timing-tracking';

describe('UserTimingTracking', () => {
  it('should create an instance', () => {
    expect(new UserTimingTracking()).toBeTruthy();
  });
});
